# 领域文档

本仓库采用单一领域上下文。

## 探索前读取

- 根目录 `CONTEXT.md`，存在时使用其中定义的领域词汇。
- `docs/adr/` 中与当前工作区域相关的架构决策。

文件不存在时继续工作，不预先创建。`CONTEXT.md` 只记录领域词汇；架构决策记录在 `docs/adr/`。

## 使用规则

- issue、规格、重构建议和测试名称应使用 `CONTEXT.md` 的既有词汇，避免使用其中明确拒绝的同义词。
- 缺失的领域词汇交由 domain-modeling 工作流确认后再写入。
- 输出与 ADR 冲突时必须明确指出，不得静默覆盖既有决策。
