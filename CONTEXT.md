# AlphaFlow 首页快照

该上下文描述 AlphaFlow 首页数据的生成、个性化与读取语义，目标是让首页始终可从已经完成的数据版本中读取。

## 语言

**默认首页快照**：
不依赖任何用户收藏偏好、可供所有用户读取的一份完整首页数据版本。
_Avoid_：公共缓存、兜底数据

**个性化首页快照**：
基于某个用户的一组冻结偏好生成、仅属于该用户的一份完整首页数据版本。
_Avoid_：用户缓存、增量快照

**偏好指纹**：
用户最近更新的自选股列表、收藏公司和收藏行业在规范化后形成的稳定内容标识。
_Avoid_：用户版本、收藏哈希

**默认基线**：
生成个性化首页快照时采用的默认首页快照版本；默认基线变化表示个性化数据应当刷新。
_Avoid_：父快照、默认缓存版本

**生成任务**：
一次确定范围、冻结偏好和目标交易日的首页快照生成请求，同一生成键只代表一次逻辑请求。
_Avoid_：刷新任务、Worker 任务
