# AlphaFlow 研究信息

该上下文描述 AlphaFlow 跨数据源、跨工作流的研究信息、个性化与读取语义。

## 语言

**专业市场基线快照**：
不依赖任何用户收藏偏好、可供所有用户读取的一份完整首页数据版本。
_Avoid_：默认首页快照、公共缓存、兜底数据

**个性化首页快照**：
基于某个用户的一组冻结偏好生成、仅属于该用户的一份完整首页数据版本。
_Avoid_：用户缓存、增量快照

**当前首页快照**：
在专业市场基线或某个用户的当前研究偏好范围内，当前获准对外服务的一份不可变首页快照；它不会因较旧生成任务晚完成而倒退，新版本尚未就绪时继续指向原有版本。
_Avoid_：最新生成快照、按生成时间取最新、可变快照

**个性化快照基线**：
生成个性化首页快照时固定采用的专业市场基线快照版本；当前专业市场基线快照变化表示个性化数据应当刷新。
_Avoid_：默认基线、父快照、默认缓存版本

**生成任务**：
一次确定范围、冻结偏好和目标交易日的首页快照生成请求，同一生成键只代表一次逻辑请求。
_Avoid_：刷新任务、Worker 任务

**调度等级**：
系统在资源饱和时用于决定任务领取机会的三档业务紧迫性：`INTERACTIVE`（交互级）、`TIME_CRITICAL`（时段关键级）和 `BACKGROUND`（后台级）。更高调度等级获得更高领取机会但不独占容量；某一等级没有可领取任务时，其空闲容量可以由其他等级借用。调度等级不等于任务生命周期状态、数据质量或分发渠道。
_Avoid_：车道、队列优先级、任务类型

**首页数据清单**：
首页快照生成所需的、带有数据集范围、必需/可选标记、目标数据截止点和获取时间上下文的完整获取清单；它固定本次获取得到的来源断言、数据观测修订、实际数据截止点和质量结果，供门控、重放和历史追溯使用。首页数据清单分为专业市场基线清单和个性化首页清单两层。
_Avoid_：首页数据需求、刷新批次、抓取任务、数据快照

**清单逻辑键**：
由清单范围、定义版本、目标上下文及个性化基线等规范化输入生成的幂等键；相同逻辑键复用同一首页数据清单，明确强制刷新才通过新的请求随机量创建新的清单历史。MVP 不以用户偏好内容的指纹作为身份字段，个性化清单直接冻结创建时的偏好内容。
_Avoid_：随机主键、任务 ID、当前快照 ID

**专业市场基线清单**：
面向所有用户共享、覆盖专业市场基线所需市场行情、资金流和全局研究事件等数据的首页数据清单；它是个性化首页清单的共享基础，不随单个用户的研究偏好变化。
_Avoid_：默认首页清单、公共数据清单、全局缓存

**个性化首页清单**：
属于单个用户、绑定创建时已经冻结的一个专业市场基线清单并追加该用户研究偏好所需数据的首页数据清单；它不复制基线清单项，也不动态跟随后续基线，个性化清单项可以独立获取和延迟完成。
_Avoid_：用户首页缓存、个性化批次、偏好快照

两类首页清单的关系约束：专业市场基线清单不属于用户且没有父清单；个性化首页清单必须属于用户并引用创建时必需项已达标的专业市场基线清单，父基线可处于 `READY_WITH_LIMITATION`，但不得是未达标清单或另一个个性化清单。

**首页数据清单项**：
首页数据清单中针对一个数据集或事实范围记录的获取结果，连接本次使用的来源断言、选定的数据观测修订、实际数据截止点和数据质量状态；它可以独立重试，但仍属于其首页数据清单。
_Avoid_：采集批次项、批次行、抓取结果、数据任务结果

**清单项逻辑键**：
由数据集、事实范围和要求版本规范化生成、并在同一首页数据清单内唯一的键；它不允许个性化追加项覆盖父基线的同范围项，需要不同口径或范围时必须生成不同键或新的清单定义版本。
_Avoid_：数组下标、Provider 请求 ID、重试 ID

**首页数据清单项获取尝试**：
针对一个首页数据清单项的一次独立 Provider 获取与结算过程，具有幂等键、租约和重试状态；同一清单项可以有多次获取尝试，成功、降级或合法空结果进入首页数据清单，错误结果依据重试归类处理。
_Avoid_：批次项任务、刷新任务、Worker 任务

**首页数据清单项结算**：
一个首页数据清单项至多一条的不可变结果记录，固定本次清单实际采用的数据观测修订、实际数据截止点、质量状态和成功、降级、合法空结果或失败结论；合法空结果是不可重试的终态，可以没有选定修订但必须保留覆盖范围和数据截止点；终态失败可以没有选定修订但必须保留错误分类和质量旗标。必需项只有正常且达到目标数据截止点的 `READY` 结算才通过，获取尝试的重试状态不改变已结算历史。
_Avoid_：尝试状态、当前抓取结果、可变批次行

清单结算只直接固定选定修订；选定来源和佐证来源通过该修订的不可变来源关联读取，失败或合法空结果结算不引用来源或修订。

**首页数据清单项修订集合**：
清单项结算固定的一个或多个数据观测修订及其顺序；单事实项可以只有一条修订，多事实数据集通过不可变关联表保存完整集合。它是首页生成和结果重放的输入，不随观测当前修订指针变化。
_Avoid_：当前值查询、动态观测集合、只保存结果哈希

**清单门控状态**：
根据同一首页数据清单的清单项结算在事务中确定的可生成性，并作为可重算的当前投影：`PENDING` 表示必需项尚未结算，`BLOCKED` 表示必需项失败或未达目标数据截止点，`READY` 表示必需项达标且可选项正常，`READY_WITH_LIMITATION` 表示必需项达标但可选项失败或降级。门控状态变化不改写清单结构、结算或修订历史。
_Avoid_：任务状态、鲜度评分、可选项失败即阻塞

**合法空结果策略**：
Provider 已成功覆盖请求范围但没有事实记录时形成的不可重试终态；清单项以 `emptyPolicy` 声明是否允许空结果，允许且覆盖完整、截止点达标时可通过门控，不允许时必需项阻塞、可选项受限。空结果不伪造数据观测修订。
_Avoid_：空结果即错误、空结果即自动通过、用零值填充

**获取尝试幂等键**：
由清单项、Provider contract/规范化版本、请求指纹和重试语义生成的全局唯一键；相同键复用同一获取尝试，新的真实重新获取才创建新的尝试序号，fencing token 防止旧 Worker 结算覆盖新结果。
_Avoid_：时间戳幂等键、重试覆盖历史、Redis 消息即状态

**首页数据清单项获取结果**：
一次首页数据清单项获取尝试向系统交付的、带版本标识的规范化结果；它固定规范化值、来源断言、实际数据截止点、数据质量状态、结构化错误和重放标识。供应商字段、单位换算和时间语义在结果进入系统前完成，结果不包含需要 C++ Worker 解释的供应商专属语义。
_Avoid_：Provider 原始响应、抓取结果 JSON、供应商返回值

**首页数据清单项获取结果状态**：
首页数据清单项获取结果的互斥交付语义：`success` 表示请求范围内的规范化值完整可用，`degraded` 表示仍有可用值但存在已声明的缺失、截止点落后或限制，`empty` 表示请求合法且成功但范围内没有记录，`error` 表示没有可交付结果并带有结构化错误及重试归类。结果状态与数据质量状态分离，`success`、`degraded` 和合法 `empty` 都是可持久化的结算终态；只有 `error` 依据重试归类决定是否再次获取。
_Avoid_：成功码、质量分、HTTP 状态、失败字符串

**首页数据清单项结果覆盖范围**：
Provider 结果对本次请求范围的结构化覆盖描述，包括 `requestedScope`、`coveredScope`、`missingScope` 和 `actualDataCutoff`；它用于表达结果缺口与事实覆盖，不直接决定清单项是必需还是可选。无任何记录的合法请求为 `empty`，部分覆盖为 `degraded`，必需项门控由数据库依据清单配置判断。
_Avoid_：返回行数、是否成功、缺失文本、抓取时间

**规范化观测值**：
Provider 结果中描述一个事实的语义记录，至少固定规范化主体、指标目录 ID、维度、观测时点或期间、带规范单位的类型化值以及缺失语义；数据集可以组织自己的类型化 payload，但不改变这些事实字段的含义。供应商字段和供应商对象 ID 只属于来源断言，不成为规范化观测值的身份。
_Avoid_：供应商行、DataFrame 行、原始字段、展示值

**规范化数值编码**：
规范化观测值中的金额、价格、比率等精确数值以十进制字符串传输，并由指标目录提供 `valueType`、规范单位和精度；不允许 `NaN`、`Infinity` 或隐式单位。缺失值使用独立的结构化原因表达，不能仅依靠 `null` 推断。
_Avoid_：二进制浮点、数量级猜单位、空值即缺失原因、格式化文本数值

**首页数据清单项结果时间分层**：
结果同时保留 `observationPeriod`、`upstreamAsOf`、`sourcePublishedAt`、`actualDataCutoff`、`normalizedAt` 和 `ingestedAt`；前四项描述业务事实或来源时间，后两项描述系统处理时间。真实时间保留来源时区并以 UTC 比较，数据截止点不等同于抓取时间。
_Avoid_：单一时间戳、抓取时间冒充截止点、入库时间冒充发生时间

**来源权威选择**：
Python Provider/适配器按“来源 × 数据集”的权威配置选择规范化值，并在结果中保留所有参与的来源断言、采用标记、权威策略版本和选择理由；C++ Worker 只校验引用并持久化，不比较来源等级、转换语义或自行回退来源。MVP 不建立跨源对账流程。
_Avoid_：全局供应商等级、Worker 选源、来源投票、隐式回退

**来源断言原始记录**：
每条来源断言保留可审计的来源原生记录或必要完整正文，以及基于稳定字节序列的内容哈希、请求参数哈希、Provider 版本和获取时间；凭据、请求头和内部日志不属于原始记录。原始记录与哈希作为不可变历史持久化，哈希不能替代审计内容。
_Avoid_：只存哈希、临时响应、带令牌的原始请求、可变缓存

**Provider 错误分类**：
Provider 使用跨来源一致的 `errorClass` 和独立 `retryability` 表达配置、授权、请求、限流、超时、上游不可用、响应无效和规范化失败等错误，并可保留供应商原始错误码、发生阶段和重试时间提示。过期、部分缺失和合法空结果由结果状态、覆盖范围和数据截止点表达，不归为错误；未知错误默认可重试并受生命周期上限约束。
_Avoid_：C++ 解析供应商错误码、过期即失败、空结果即异常、错误字符串分支

**Provider 重放上下文**：
每个获取结果固定 `acquisitionAttemptId`、幂等键、请求指纹、Provider/规范化规则版本、来源内容哈希、规范化结果哈希和首次获取、同一尝试重试或新尝试重新获取的模式。结果重建重放不调用 Provider，也不创建新的获取结果或修订。
_Avoid_：重放即重新抓取、时间戳即幂等键、重复响应即新修订、内存重试标记

**Provider 数据集能力**：
每个 Provider 适配器声明可处理的 `datasetKey` 与版本化能力，并通过统一的首页数据清单项获取结果信封交付；不支持的数据集返回结构化 `unsupported_dataset`。TuShare、Minishare 和测试替身共享同一组契约测试，测试替身可以脚本化成功、降级、空结果、错误、缺口、截止点和重复响应。
_Avoid_：动态透传供应商接口、Provider 专属返回形状、只测固定成功样本

**Provider contract 版本**：
Provider 结果使用显式 `major.minor` 版本，并同时记录数据集 payload 版本和规范化规则版本；破坏字段语义、单位、时间或状态枚举时递增主版本，只增加可选字段时递增次版本。消费者不对不支持的主版本做隐式转换，返回 `contract_incompatible`；项目未部署期间不保留长期双版本兼容层。
_Avoid_：无版本 JSON、静默单位转换、未知主版本强行解析、永久双写

**研究模式**：
面向专业 A 股投资者提供个性化研究材料、证据、观点变化、待验证条件和风险提示，但不结合个人资产或持仓给出买卖动作、仓位或价格指令的产品模式。
_Avoid_：荐股模式、投顾模式、交易建议模式

**个性化研究雷达**：
持续追踪投资者明确关注的公司、行业、主题与研究假设，只呈现具有实质影响的新变化，并说明变化的相关性、证据、观点影响和后续验证项；它作为独立区域增量提升相关内容，不重排或删减专业市场基线，也不能降低全局高重要性研究事件的可见性。
_Avoid_：资讯聚合、新闻推送、智能荐股

**专业市场基线**：
无需用户预先录入偏好即可使用、按盘前、盘中、盘后和前瞻工作节奏组织的全市场研究工作台；其内容集合与内部排序对所有用户一致，每个时段保留全市场重要事件的固定展示容量。个性化内容通过独立的个性化研究雷达和研究收件箱视图呈现，不改变基线的内容、顺序和完整性。
_Avoid_：默认资讯流、公共首页、兜底内容

**数据观测**：
某个规范化主体的一个指标、维度集合在特定时点或期间上的可追溯状态，是外部数据进入研究分析与分发流程后的最小可复用研究对象之一。其事实身份由主体、指标、维度和观测时段组成；来源、抓取时间、质量、鲜度和修订信息属于证据与版本元数据，不改变事实身份。多个来源指向同一事实时归并为同一数据观测，更正通过新修订并保留与旧值的关系表达。
_Avoid_：市场观测、行情异动、数据事件

**规范化身份键**：
由规范化器根据主体、指标、已排序的维度集合和观测期间生成的版本化稳定键；它只描述数据观测的事实身份，不包含来源、抓取时间或当前取值，并与规范化维度 JSON 一起保存以支持唯一约束和追溯。
_Avoid_：来源键、抓取键、值哈希

**来源断言**：
某一外部数据源对一个数据观测给出的独立事实陈述，保留来源标识、原始记录、获取时间和血缘，可与其他来源断言比较或产生冲突；同一来源记录以来源、数据集、记录身份和原始内容哈希幂等，不同来源即使内容相同也保留独立断言。来源断言不是额外的数据观测，也不因异常自动成为研究事件。
_Avoid_：来源数据、原始观测、数据副本

**来源权威**：
按来源与数据集确定的取值优先级。规范化数据观测优先采用该数据集声明的权威来源；权威来源缺失时，允许使用已声明的低权威回退来源，并向用户作简单的非主来源/回退提示。MVP 不建立额外的冲突状态或跨源对账流程，但保留来源断言和选源依据供追溯。
_Avoid_：全局可信供应商、来源投票、冲突工作流

选定来源、回退原因和权威策略版本随数据观测修订冻结；主来源后来以相同规范化值到达时不回写历史修订，后续值变化或明确策略/更正要求才形成新修订。

**指标目录**：
规范化指标的共同语义目录，定义指标含义、允许的主体与维度、数值类型、规范单位、精度、缺失值语义以及可执行的确定性换算和来源字段映射。目录未声明的单位、口径或换算不能被猜测为规范化指标。
_Avoid_：供应商字段名、展示标签、按数量级猜单位

**数据质量状态**：
数据观测经过最低基线校验后的可解释状态：正常、降级或隔离。状态由结构、类型、主体与期间识别、单位、必填值、明显非法值和权利状态等事实决定，并附带具体旗标与限制；MVP 不计算总质量分，不把数据质量与 LLM 置信度或分发评估混合。
_Avoid_：总质量分、质量即置信度、质量即鲜度

清单项 `READY/DEGRADED/FAILED` 是独立的结算门控结果：只有存在选定修订、观测质量正常且实际数据截止点达到目标才为 `READY`；质量降级、覆盖不足或截止点落后为 `DEGRADED`，隔离或没有可用修订为 `FAILED`。

**事件数据证据准入**：
数据观测只要不处于降级、隔离、过期、单位未知或主体/期间不明状态，就可以作为研究事件晋级证据；价格、成交量等异常仍只能先形成候选，不能绕过现实变化成立和研究价值成立的裁定。
_Avoid_：复杂质量门槛、异常即事件、质量分晋级

**观测时间语义**：
数据观测同时保留指标所描述的观测时段、上游声明的截点或快照时间、来源公开该记录的时间、系统收到原始记录的时间和完成规范化的时间；这些时间不能互相替代。真实时间保留来源时区并以 UTC 作为统一比较基准，交易日和报告期等日期/期间语义不因时区转换而改变。按历史时点回溯时，以来源可见时间判断信息当时是否可用，而不是以观测时段或系统接收时间代替。
_Avoid_：单一时间戳、入库时间即发生时间、接口更新时间即 SLA

**数据截止点**：
某个数据集当前已经完整覆盖到的最新业务时点或期间，用于判断采集结果是否达到目标，而不是表示系统抓取时间。数据截止点的含义由数据集明确，例如最新完整交易日、最新报告期或最新发布时间。
_Avoid_：最新抓取时间

**数据鲜度规则**：
按数据集配置的预计可用时间与目标数据截止点规则；系统通过比较实际数据截止点与目标数据截止点判断是否按期更新，不采用复杂算法或评分模型。只有同一数据集在不同来源或交易阶段存在实际差异时，规则才进一步按来源或交易阶段细分。
_Avoid_：鲜度评分、统一硬 SLA、默认按来源和交易阶段拆分

**数据观测修订**：
同一数据观测的规范化选值发生变化或上游明确更正时产生的新版本，并指向被替代版本；每个观测以单调修订序号和替代链表达版本顺序，并以当前修订指针作为读取投影。重复抓取、抓取时间变化或未影响选值的来源更新不产生修订。无明确更正时，修订按观测、规范化值哈希和规范化版本幂等；明确更正即使值回到历史值，也创建新的修订节点。每个修订通过不可变来源关联固定一个选定来源断言，并可保留多个佐证断言。首页快照、Agent 输入和报告固定其实际使用的修订，后续修订不改写历史产物。
_Avoid_：覆盖更新、每次抓取一个版本、动态历史快照

清单项结算另外保存目标数据截止点与实际数据截止点；门控只比较这两个业务截止点，不以来源发布时间、系统入库时间或通用更新时间替代。

**观测血缘**：
数据观测修订与其直接来源之间的可追溯关系，包括采用的来源断言，或派生指标直接依赖的观测修订、规则版本和必要参数；首页快照、Agent 输入和报告通过首页数据清单或证据快照固定其使用集合。血缘只记录直接父级，不预先展开完整传递依赖图。
_Avoid_：完整调用轨迹、逐步骤日志、全量血缘图

派生修订的直接输入使用独立关联记录，原始修订的来源断言使用带角色的来源关联；两者均不可变，并禁止自指或环路。

**探索读取**：
为筛选、财务指标详情、K 线和其他数据浏览提供的临时结果，只需完成参数、权限、返回结构、数值类型、报告期和单位等轻量校验；它保留来源和读取时间，但不是可复用的数据观测，不要求跨源对账、完整血缘、修订合并或深度质量评分。
_Avoid_：研究快照、已验证观测、临时结果即研究证据

**研究持久化**：
用户明确保存为研究材料，或交给 Agent、报告流程形成可复用研究输出时触发的对象升级；升级后的数据必须成为规范化数据观测并保留相应证据、版本/修订关系和血缘。导出功能不属于当前产品范围。
_Avoid_：保存即缓存、Agent 输入即原始数据、导出快照

**事件生产**：
将全局研究事件候选核实为可跨工作流复用的研究事件，并为专业市场基线、研究收件箱和定时简报等预生成内容提供输入；其结论不因用户数量或个别用户研究偏好而改变。
_Avoid_：事件推送、用户事件生成、新闻加工

**即时研究**：
用户直接调用研究智能体、公司研究或行业研究时，围绕当前问题使用有限证据并经过准确性核查形成的快速研究输出；它固定本次使用的证据，但不要求先经过完整事件生产，也不自动创建或修改全局研究事件。即时研究不为复用而主动查询、匹配或等待权威对象；正常检索恰好命中既有数据观测、研究事件或证据修订时才采用，否则不采用权威对象并继续推进当次研究。新发现可在响应后作为候选种子异步、幂等地回灌事件生产。
_Avoid_：即时事件、快速事件管线、未审核研究事件

即时研究默认只读：它可以读取冻结的数据观测、研究事件、证据、研究偏好和研究收件箱记录，并在 Agent 执行边界内进行开放网页发现；输出必须保留来源、数据截止点和不确定性。回答文本本身不能修改研究关注、持续追踪计划、研究收件箱状态或分发安排，任何副作用都交由确定性意图处理流程执行。

研究智能体由 Harness 按会话持久化、恢复和压缩上下文，可以在缺少会改变研究方向的必要信息时暂停并逐问获取用户输入；只影响细节时应声明假设并继续。后台研究工作流、事件裁定、分发评估、简报、持续追踪和定时任务不等待用户输入，必要输入缺失时返回结构化缺口并结束或暂缓。

用户要求买卖、持有、加减仓、仓位、入场价、目标价、止损价或订单计划时，确定性 `research_only` 输出约束拒绝执行性部分，研究智能体不为此追问持仓或风险偏好；它仍可提供相关事实与证据、不确定性、正反情景、风险因素、判断条件和后续验证项。Skill、网页检索、附件或其他输出形态不能绕过该约束。

**候选种子**：
即时研究向事件生产提交的现实变化线索与证据集合；它只用于触发或补充研究事件候选，不代表即时研究已经完成全局晋级裁定。
_Avoid_：即时事件、Agent 事件、已核实事件

**研究事件**：
现实中一次可区分、已经发生且对专业市场基线具有研究价值的规范化变化，说明发生了什么、为什么值得关注并保留可追溯证据，是全局共享的最小可复用研究对象之一。同一事实的明确更正形成该事件的新修订，后来发生的新决策或新披露形成新的关联事件；异常数据本身不自动构成研究事件，用户研究偏好也不决定事件是否存在。
_Avoid_：新闻、资讯、异动事件

**研究事件修订**：
同一研究事件的事实主张、研究含义、证据、不确定性或核实状态发生实质变化时形成的不可变版本，并指向被替代版本。已晋级事件只能形成更正、重新核实或撤回修订，不能退回候选的驳回状态；更正与撤回不覆盖历史修订或据此生成的历史内容，技术失败也不改变当前有效修订。当前读取使用最新有效修订，历史内容同时提示其依据后来发生的更正或撤回。
_Avoid_：覆盖更新、删除事件、改写历史内容

**研究事件候选**：
由一项或多项外部事实、数据观测触发，代表一个可能的现实变化，并等待核实“现实变化成立”和“对专业市场基线的研究价值成立”的全局暂存对象；原始材料是候选的独立证据，不因聚合而丢失。它不是研究事件。异常数据只能触发候选，不能独自证明现实变化，用户研究偏好不参与候选晋级。暂缓候选可以作为“待核实变化”展示，但必须同时标明暂缓状态、证据缺口和后续核实条件，不得伪装成已确认的研究事件。
_Avoid_：未审核事件、新闻事件、异常事件

**候选簇**：
经精确去重和语义聚合后，默认代表一个现实变化的一组候选材料及其证据；仅当材料明确包含多个可独立成立且时间或事件主体不同的变化时，才拆分为多个研究事件候选。
_Avoid_：新闻集合、主题集合、事件列表

**候选裁定**：
LLM 按固定规则对研究事件候选给出的结构化三态结论：晋级表示两道门槛均成立并形成研究事件；暂缓表示证据不足、冲突或资格不明，保留候选等待新证据；驳回表示已明确不成立或只是重复信息，保留裁定记录但不主动重试。
_Avoid_：审核结果、事件状态、置信度等级

**候选终结**：
晋级或驳回使当前候选簇及其证据集合不再自动重试；暂缓保持开放并等待声明的核实条件、观察窗口或实质证据增量。新独立证据在驳回后应创建并关联新候选，不修改原裁定。
_Avoid_：删除候选、覆盖裁定、无限重试

**事件证据资格**：
某项证据证明特定事实的能力，必须同时确认来源身份真实，以及该来源有资格证明该项事实；来源真实不等于对所有事实均有证明资格。权威一手证据可独立证明事实，否则需要相互独立的证据交叉印证。
_Avoid_：权威来源、可信网站、来源等级

**证据独立性**：
两项证据是否来自彼此独立的事实采集链，而不是发布渠道或域名是否不同；转载同一公告、通讯社稿件、匿名爆料或相互引用的材料只算同一事实来源。共同上游无法识别时为“独立性未知”，不能满足交叉印证门槛。
_Avoid_：多家媒体、多网址印证、来源数量

**来源身份依据**：
系统提供、用于判断网址、账号或接口是否属于其声称主体的可审计依据；交易所、监管机构、认证账号和可信接口采用固定配置，公司网址则根据注册域名的 ICP 备案主体是否与公司法定全称一致，区分“大概率官网”和“未核实”。LLM 只能依据随输入提供的身份依据裁定真实性，不能凭模型记忆认定来源身份。
_Avoid_：全量官网白名单、模型常识、可信域名猜测

**大概率官网**：
注册域名的工信部 ICP 备案主体与公司法定全称一致的公司网址身份状态；它表示来源身份通过初期规则判断，不表示该来源有资格证明任意事实。
_Avoid_：已认证官网、官方来源、权威来源

**事件事实主张**：
研究事件修订中可独立核实的原子陈述，逐条绑定引用片段、来源指针或数据观测修订，并记录来源身份真实性与事实证明资格；事件证据不保存原始全文，研究含义只能引用这些主张并明确标为推断。
_Avoid_：事件摘要、LLM 结论、事实与观点混写

**事件主体**：
现实变化直接发生其上的规范化公司、行业、监管机构、宏观指标或主题，是研究事件事实契约的一部分；文本提及某个实体不等于该实体是事件主体。
_Avoid_：相关实体、命中对象、受影响标的

**影响对象**：
经影响分析判断可能因研究事件受益、受损或需要关注的公司、行业或主题；它可以随证据和分析修订，但不参与研究事件身份。
_Avoid_：事件主体、关联标的、推荐标的

**用户相关对象**：
影响对象与某个用户研究偏好的匹配结果，只用于个性化相关性、排序和分发，不属于全局研究事件。
_Avoid_：事件实体、用户事件、推荐对象

**研究偏好**：
由投资者主动设置、可查看、修改和清除的个性化研究配置；MVP 只由研究关注和用户级分发开关组成，不根据查看、搜索、展开、稍后、有用或噪声等行为推断兴趣。研究偏好不屏蔽专业市场基线。
_Avoid_：用户画像、推荐画像、兴趣标签、行为偏好

关闭个性化只暂停使用研究关注进行排序与分发，保留配置以便恢复。清除研究偏好会删除全部研究关注并停止未来的个性化分发，但不删除专业市场基线或既有研究收件箱记录；最终确认后不可恢复。移除单个研究关注只影响该对象未来的个性化排序与分发，历史记录继续保留。这三种动作不能互相替代；关闭和单项移除应提供当场撤销。

研究偏好变更只作用于变更后新进入个性化评估与分发流程的内容。已经形成待发送记录或进入发送队列的内容继续按其冻结的研究偏好和分发结果正常发送，不重新门控、取消或改写；后续内容使用新的研究偏好。MVP 不引入在途内容回查。

“稍后”只管理阅读状态，“有用 / 噪声”只保存单条分发质量反馈；MVP 不将这些动作或查看、搜索、展开行为复制为个性化行为历史，也不用于个性化排序、相关性评估或主动分发。

**研究关注**：
投资者主动确认需要持续追踪的公司、行业、主题、研究事件或研究假设；它可以影响个性化相关性、排序，并使相关内容因个性化进入研究收件箱、定时简报或紧急提醒候选。研究关注只能由投资者明确添加、修改或移除，不能由查看、搜索等行为自动创建。全局高重要性研究事件仍可依据统一分发规则进入研究收件箱，不依赖研究关注。
_Avoid_：持仓、推荐标的、自动关注、兴趣标签

**研究关注级别**：
投资者为单个研究关注选择的个性化分发强度，只有 `常规关注` 与 `重点关注` 两档。新增或导入对象默认为常规关注，可进入个性化研究雷达、研究收件箱和定时简报，但不能仅凭命中关注而取得紧急提醒资格；重点关注由投资者主动提升，在同时满足重要性、置信度和信息增量门槛时才可进入紧急提醒候选。关注级别不改变研究事件的全局重要性，也不表示持仓或投资意向。
_Avoid_：投资评级、持仓等级、风险偏好、自动重点关注

MVP 不为单个研究关注配置分发渠道矩阵。常规关注固定作用于个性化研究雷达、研究收件箱和定时简报；重点关注在此基础上取得紧急提醒候选资格。紧急提醒、定时简报和外部提醒副本只提供用户级总开关；不希望某个对象继续参与个性化主动分发时，应移除对应研究关注。

**研究关注层级传播**：
研究关注通过结构化对象关系与研究事件影响关系传播相关性，但不传播研究关注身份或关注级别。行业或主题受到关注时，涉及该对象整体的研究事件直接命中；其下级公司事件只有对该行业或主题具有明确实质影响时才以较弱相关性进入个性化研究雷达或定时简报，不能继承重点关注资格或据此触发公司级紧急提醒。公司受到关注时，可以向对象目录中明确登记的所属行业传播弱相关性，但不自动创建行业研究关注。MVP 不由 LLM 猜测对象归属或传播关系。
_Avoid_：关注继承、行业关注等于成分公司关注、自动扩大关注范围

收藏公司、收藏行业和自选股保留各自原有用途，不自动成为研究关注。首次使用个性化研究雷达时，系统可以把这些既有对象列为可选导入项，但必须说明导入对研究雷达、定时简报和紧急提醒的影响，并由投资者明确确认；未导入对象不参与主动分发。没有研究关注时，投资者仍可直接使用完整的专业市场基线。

**研究偏好快照**：
某次个性化相关性评估或分发门控开始时，从用户当前启用状态、用户级分发开关和研究关注集合冻结形成的不可变输入；它记录契约版本、规范化关注对象与级别、冻结时间和内容哈希。相同内容可以幂等复用，后续偏好修改不改写既有快照。MVP 不另建偏好版本、偏好指纹或其他偏好身份键。
_Avoid_：当前偏好查询、用户画像、行为快照、可变偏好记录

**个性化依据解释**：
说明某次个性化排序或分发实际命中了哪个研究关注、命中关系和当时关注级别的按需审计信息。它通过内容详情入口查询，不在每条个性化内容上常驻展示；集中式研究偏好页面列出当前研究关注并允许修改级别或移除。纠正只影响未来排序与分发，不重写历史内容。
_Avoid_：猜你喜欢、常驻推荐理由、不可追溯的相关性标签

内容上的“不再关注”必须依据该内容实际命中的研究关注精确执行：只有一个命中项时，确认层明确显示将移除的公司、行业、主题、研究事件或研究假设；存在多个命中项时由投资者选择，系统不得猜测。移除上级研究关注不影响投资者另行明确添加的下级研究关注。

**紧急提醒**：
高重要性、高置信度、高信息增量且直接命中重点关注的研究事件，可主动打扰投资者。暂缓候选在来源身份真实、至少一项证据有资格证明核心事实、直接命中重点关注且潜在影响重大时也可发送，但必须醒目标记“待核实”并说明证据缺口或冲突；常规关注和层级传播相关性不能单独取得个性化紧急提醒资格。异常数据、匿名传闻和来源身份未核实的信息不得主动提醒。MVP 暂不引入跨时间去重、冷却和频率约束。
_Avoid_：实时推送、异动提醒、告警

**定时简报**：
按专业工作节奏汇总的重要但不紧急的研究事件和显著数据观测变化。
_Avoid_：日报、新闻摘要、消息汇总

**相关研究认知基线**：
系统在分发评估时冻结的、已经成立且仍有效，并与当前研究事件修订相关的研究认知集合；它包括同一事件的历史修订、语义重复内容、明确关联事件及已有关键事实，不限于上一份简报或研究收件箱内容。
_Avoid_：上一条消息、收件箱历史、上一份简报

**信息增量**：
当前研究事件修订相对于相关研究认知基线，新增了多少会影响研究判断的实质信息；比较结果属于分发评估的一部分，不等同于文本差异、发布时间新旧或消息是否首次送达。
_Avoid_：文本新颖度、发布时间、首次推送、收件箱增量

**分发评估**：
研究事件在分发前形成的重要性、置信度、相关性和信息增量四项判断；每项都包含分数、依据、引用证据和不确定性，最终分发由确定性规则执行。
_Avoid_：推荐分、新闻评分、LLM 决策

**研究收件箱**：
集中接收、阅读和管理紧急提醒与定时简报的站内权威记录，并保留内容与研究对象、证据、生成和修订记录之间的关系；外部渠道只发送可关闭的提醒副本。
_Avoid_：私信、通知中心、消息列表

用户开启某个外部提醒渠道并完成必要授权后，形成持续预授权；通过确定性分发门控的紧急提醒、定时简报、更正和撤回通知可以自动发送，无需逐条确认。Agent 不选择收件人、渠道或发送时机，也不直接调用外部发送工具；新增渠道、扩大接收范围、凭据重新授权或恢复已关闭渠道必须再次由用户确认。

**核心投资者**：
以个人身份使用产品、管理较集中 A 股组合的全职或高投入个人投资者；具备财报和行业研究能力，但没有机构级研究助理与信息系统。
_Avoid_：普通散户、机构客户、投顾客户

**Agentic Workflow**：
Agent 在确定性编排授予的冻结输入、工具和资源边界内生成结构化候选、分析或草稿；这些结果本身不具有权威写入、领域状态迁移、`research_only` 输出约束或分发门控、投递及其他外部副作用权限。确定性控制验证并接管后续动作，通过门控的常规 `research_only` 内容无需逐条人工批准。MVP 不在工作流内判定数据权利或整体合规资格。
_Avoid_：自主写库、自主发布、Agent 即权威状态、Agent 自行放行

研究事件裁定、分发评估和简报表达中的语义判断由 Agent 或 LLM 完成；确定性控制只校验结构、引用闭合、版本兼容和执行边界，不重复进行同一业务判断。合法结果可由代码自动结算为相应领域状态，无需用户逐项确认。

**开放网页发现**：
Agent 可以通过受控抓取能力自主发现并访问任意公开 HTTP(S) 网页，采集阶段不以来源可信度白名单预先拦截。公开网页材料进入系统时仍只是来源身份与证据资格尚未裁定的候选材料；访问控制、内网隔离、凭据保护以及后续证据裁定由确定性控制执行，Agent 不得绕过登录、付费墙或其他访问限制。MVP 不在采集流程内判定材料的数据权利或整体合规资格。
_Avoid_：可信域名白名单、任意网络访问、网页内容即可信证据、抓取结果直接入权威对象

**证据补全任务**：
研究事件候选裁定前，由 Agent 对公开网页进行有界的多轮检索、反证查找和来源身份线索补充，并在结束时冻结本次采用的证据集合。研究事件裁定只读取该冻结集合并执行一次；裁定期间不继续动态扩充证据，后来发现的实质证据通过新的候选修订触发重裁定。
_Avoid_：边检索边裁定、无限调查、动态裁定输入、补全任务直接晋级事件

**Agent 执行边界**：
确定性控制为一次 Agent 运行冻结的目标、输入快照、幂等身份、工具与网络权限，以及步骤、时间、Token、成本和并发上限。Agent 可以在边界内决定执行顺序和提前结束，但不能自行扩大目标、权限或预算；额度耗尽时返回已有结果、证据缺口和停止原因，由确定性控制决定是否创建新任务。
_Avoid_：自主扩权、自主续期、无限循环、隐式子任务、Agent 修改停止条件

确定性控制按任务授予能力集合及约束，Agent 可以在集合内按需选择和组合 Skill、工具与调用顺序。Skill 声明的权限不等于系统授权；Agent 不得安装或修改 Skill、执行未登记命令、取得新凭据或自行启用集合外能力。

Agent 可以在同一次执行边界内拆分并发子任务；子任务继承父任务的能力集合并共享父任务的步骤、时间、Token、成本、工具调用和并发上限，不能通过委派增加额度或权限。子任务只向父任务返回结构化结果，不独立写入权威状态、分发内容或建立持续追踪；后台任务仍以单一父任务结果和冻结证据集合结算。

**Agent 运行审计**：
对一次 Agent 运行保存任务身份与发起者、父子任务关系、输入快照或哈希、Agent 执行边界、Skill/工具/模型版本、工具调用摘要与来源、最终结构化输出、契约校验、停止原因、资源消耗以及由结果触发的领域对象或意图处理记录。审计不保存隐藏推理过程，也不要求永久保存所有网页全文；研究智能体会话由 Harness 按其会话机制持久化。
_Avoid_：思维链日志、只存最终文本、网页全文永久归档、不可关联的工具日志

**简报编排**：
Agent 在确定性控制已经冻结的必选与可选研究事件修订范围内组织叙事、合并上下文并提炼变化、风险和后续验证项。选材资格、必显更正或撤回、渠道、容量和研究收件箱写入顺序不由 Agent 改变；简报中的事实主张继续引用冻结的研究事件与证据。
_Avoid_：Agent 自主选材、简报新增事实、隐藏更正或撤回、先发送外部副本

**后台研究工作流**：
生成专业市场基线、个性化研究雷达、研究收件箱、定时简报或紧急提醒的主动流程；它只能消费已经结算的权威数据观测修订、研究事件修订、证据、分发评估和冻结研究偏好。Agent 从网页材料形成的候选或分析必须先经过候选裁定和确定性写入，不能作为临时材料直接进入后台产物；即时研究是允许直接采用当次网页证据形成用户回答的例外，但不能据此主动分发。
_Avoid_：网页结果直达简报、即时证据主动推送、后台临时事实、Agent 绕过权威对象

**持续追踪计划**：
描述待验证事实、合格证据、触发条件、检查频率、到期时间和资源上限的结构化计划。研究事件核实所需的全局计划可由确定性控制自动激活；个性化计划只能来自用户已经明确建立的研究关注或当次确认。计划命中只产生新证据或候选修订，不直接形成研究事件或用户分发。
_Avoid_：Agent 长期后台任务、兴趣线索自动关注、命中即提醒、无到期追踪

**人在回路**：
用户对涉及自身的未预授权外部副作用、研究偏好变更和高风险操作进行明确确认的控制点；它是用户授权机制，不是正常研究内容的逐条审批流程。MVP 不建设运营人工复核或异常处置队列；证据冲突和技术失败等可判定异常由确定性控制自动暂缓、降级或阻断，也不建设运行时数据权利或整体合规资格判定机制。
_Avoid_：全量人工审核、默认人工批准、运营审核队列、用人工确认替代服务端门控

**确定性意图处理**：
代码将 Agent 提出的结构化用户意图解析为唯一领域对象，执行权限、前置条件和确认等级校验后再写入权威状态。用户明确、单义且可撤销的自然语言指令本身可以作为确认；对象含糊、批量变更、扩大外部分发、访问受限资源或不可恢复操作必须先展示精确影响并取得二次确认。
_Avoid_：回答文本即副作用、Agent 直接写入、含糊对象自动选择、统一二次确认
